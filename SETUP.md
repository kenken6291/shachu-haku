# くるま旅マップ 会員機能 セットアップ手順

## 1. スプレッドシート & GASプロジェクトの作成
1. 新規のGoogleスプレッドシートを作成（例: `shachu-haku-db`）
2. [拡張機能] → [Apps Script] を開く
3. デフォルトの `Code.gs` の中身を、このリポジトリの `Code.gs` の内容に置き換える
4. スクリプトエディタ上部の関数選択で `setupSheets` を選び、▶実行
   - `Users` / `Spots` / `Comments` / `Favorites` の4シートが自動生成されます
   - 実行時にGoogleの権限承認ダイアログが出るので許可してください
   - ※すでに `Users`/`Spots`/`Comments` が存在するスプレッドシートを更新する場合は、
     手順7「機能追加時のマイグレーション」も行ってください

## 2. スクリプトプロパティの設定
1. スクリプトエディタの左メニュー [プロジェクトの設定] を開く
2. [スクリプト プロパティ] → [プロパティを追加] で、以下をそれぞれ追加

   | プロパティ | 値 |
   |---|---|
   | `PEPPER` | パスワードハッシュ用の適当な長いランダム文字列（例: 32文字程度） |
   | `PHOTOS_FOLDER_ID` | 写真保存用Googleドライブフォルダの ID（下記3を参照） |
   | `GEMINI_API_KEY` | 音声入力の文字起こし・整形に使うGemini APIキー（下記4を参照） |

3. 保存

## 3. 写真保存用Googleドライブフォルダの準備
1. Googleドライブに新規フォルダを作成（例: `shachu-haku-photos`）
2. フォルダを開き、URLの末尾の文字列（`https://drive.google.com/drive/folders/【ここ】`）をコピー
3. 上記「スクリプトプロパティ」の `PHOTOS_FOLDER_ID` にこの値を設定
4. アップロードされた写真は自動的に「リンクを知っている全員が閲覧可」に設定されます（サイト上で表示するため）
   - フォルダ自体の共有設定を変更する必要はありません（ファイル単位で共有されます）

## 4. Gemini APIキーの取得（音声入力機能）
1. [Google AI Studio](https://aistudio.google.com/) にアクセスし、APIキーを発行
2. 上記「スクリプトプロパティ」の `GEMINI_API_KEY` にこの値を設定
3. 音声入力はログイン済み会員のみ利用可能です（API利用量を抑えるため）
4. ブラウザの録音形式（`audio/webm` 等）によっては、Gemini API側で認識精度が変わる場合があります。
   うまく認識されない場合は録音環境（静かな場所・はっきりした発話）をお試しください

## 5. ウェブアプリとしてデプロイ
1. [デプロイ] → [新しいデプロイ]
2. 種類の選択で「ウェブアプリ」を選ぶ
3. 設定:
   - 実行するユーザー: **自分**
   - アクセスできるユーザー: **全員**
4. デプロイ後に表示されるURL（`https://script.google.com/macros/s/xxxxx/exec`）をコピー
5. 初回実行時、Gmail送信に加えて**Googleドライブへのアクセス権限**の承認ダイアログが出るので許可してください

## 6. フロントエンドの設定
`index.html` 内の以下の行を、手順5で取得したURLに書き換えてください。

```js
gasUrl: 'https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec',
```

## 7. 機能追加時のマイグレーション（既存デプロイの場合のみ）
以前から運用しているスプレッドシートに今回の機能を追加する場合、`setupSheets()` では
既存シートへの列追加は行われないため、スクリプトエディタで以下の関数を**一度だけ手動実行**してください
（関数選択で選び ▶実行）。

| 関数 | 追加される内容 |
|---|---|
| `migrateAddSleepStatusColumn` | `Spots` シートに `sleepStatus` 列（車中泊の可否） |
| `migrateAddPhotosColumn` | `Spots` シートに `photos` 列（写真のドライブファイルID） |
| `migrateAddRatingColumn` | `Comments` シートに `rating` 列（評価） |
| `migrateAddFavoritesSheet` | `Favorites` シート自体（お気に入り機能） |

実行後は、[デプロイ] を「編集」して**新しいバージョン**を作成しないと公開URLに反映されません。

## 8. 注意事項・免責事項の文面調整
`index.html` 内の `TERMS_TEXT` 定数に、会員登録時に同意させる注意事項・免責事項の文面が入っています。
サイトの実態に合わせて内容を調整してください。

## 9. 動作確認
- 会員登録 → 届いたメールの仮パスワードでログイン → 強制的にパスワード変更画面が出るか
- マイページからニックネーム変更・パスワード変更ができるか
- パスワードを忘れた方 → 仮パスワードが再度メールで届くか
- スポットを投稿 → 自分の投稿にのみ「編集する」「削除する」ボタンが出るか
- コメント投稿・評価 → 自分のコメントにのみ「編集」「削除」が出るか、平均評価がカード・ポップアップに出るか
- お気に入り登録 → サイドバー／シートの「⭐お気に入りのみ表示」で絞り込めるか
- ルートに追加 → 複数スポットを追加し、並べ替え・Googleマップでの複数地点ルート表示ができるか
- 写真投稿 → 選択した写真が自動的にリサイズされてアップロードされ、ポップアップにサムネイル表示されるか
- 音声入力 → 録音→Gemini整形後のテキストが表示され、「追加する／置き換える／破棄」がそれぞれ動作するか

## GAS更新時の注意
GASのコードを更新した際は、既存の [デプロイ] を「編集」して**新しいバージョン**を作成しないと、
公開URLに変更が反映されません（Kenさんの他プロジェクトと同様の注意点です）。

## データ構成のメモ
- 既存の `spots.json`（GitHub管理）はそのまま「運営者が管理する基本スポット」として残り、
  地図には「静的データ（spots.json）」＋「会員投稿データ（スプレッドシート）」がマージされて表示されます。
- 静的データ側には編集・削除ボタンの代わりに、従来どおり「この情報を修正する」→GitHub編集リンクが出ます。
- 会員投稿データ側には投稿者本人のみに編集・削除ボタンが表示されます。
- 周遊ルート機能はブラウザの`localStorage`にのみ保存される端末ローカルの機能で、サーバー側にデータは残りません。
- 写真は1スポットにつき最大3枚まで。クライアント側で長辺1600px・JPEG品質0.82程度に自動圧縮してからアップロードします。

